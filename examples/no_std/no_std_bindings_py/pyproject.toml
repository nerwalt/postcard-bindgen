
[project]
name = "no_std_bindings_py"
version = "0.1.0"
authors = [
  { name="postcard-bindgen" },
]
description = "Auto generated bindings for postcard format serializing and deserializing python to and from bytes."
requires-python = ">=3.8"
